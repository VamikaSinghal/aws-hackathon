{
  "name": "adaptive-health",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous health optimization agent for the Loop Engineering Hackathon.",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1090.0",
    "@aws-sdk/lib-dynamodb": "^3.1090.0"
  }
}
